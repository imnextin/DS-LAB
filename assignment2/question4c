#include <iostream>
#include <cctype>
using namespace std;
int main(){
    string s; getline(cin,s);
    for(char c:s){
        char x=tolower(c);
        if(x!='a'&&x!='e'&&x!='i'&&x!='o'&&x!='u') cout<<c;
    }
}
